# Cuda Inference tool

### Usage:

##### Compile and run:

```
mkdir ./build && cd build
cmake ..
make
```

##### Compile and test:

```
mkdir ./build && cd build
cmake ..
ctest
```


### TODO List:

- Test if using inplace operations (for example for relu) is faster